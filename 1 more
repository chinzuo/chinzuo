#include <iostream>
#include <cstdlib>
#include <ctime>

int main() {
    // Seed the random number generator
    std::srand(std::time(nullptr));

    // Generate and print a random number between 1 and 1000
    int randomNumber = std::rand() % 1000 + 1;
    std::cout << "Random number: " << randomNumber << std::endl;

    return 0;
}


